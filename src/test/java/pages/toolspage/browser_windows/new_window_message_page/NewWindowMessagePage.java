package pages.toolspage.browser_windows.new_window_message_page;

import org.openqa.selenium.WebDriver;
import pages.toolspage.browser_windows.CommonWindow;

public class NewWindowMessagePage extends CommonWindow {
    public NewWindowMessagePage(WebDriver driver) {
        super(driver);
    }
}
